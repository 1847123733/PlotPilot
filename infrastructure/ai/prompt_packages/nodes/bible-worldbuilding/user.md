【故事创意】
{premise}

【目标章节数】
{target_chapters} 章

【已有设定】
{existing_settings}

---

请严格按以下 JSON 模板输出。**每个字段的值只能是中文段落字符串，禁止嵌套 JSON 对象或数组，禁止出现英文键名**（如 name、tier、description、cost 等）。境界层次、地域列表、势力关系等用【小标题】写进一段正文里。

```json
{{
  "worldbuilding": {{
{fields_desc}
  }}
}}
```

